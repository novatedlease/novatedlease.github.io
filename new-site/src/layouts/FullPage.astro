---
import Base from './Base.astro';

interface Props {
  title?: string;
  description?: string;
  extraSchemas?: object[];
}

const { title, description, extraSchemas } = Astro.props;
---

<Base title={title} description={description} extraSchemas={extraSchemas}>
  <slot />
</Base>
